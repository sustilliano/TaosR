# CLAUDE.md

Read `AGENTS.md` — the planekey work loop defined there is the
operating contract for this repo: baseline snapshot, scoped additive
change, tests, after-snapshot + compare, repoguard scan, report the
delta with counts.
